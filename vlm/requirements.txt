# Dedicated environment for the visual-instruction-tuning (LLaVA) track.
# This pin set is *intentionally older* — it matches LLaVA-v1.5's environment.
#
# Recommended setup:
#     python -m venv .venv-vlm
#     source .venv-vlm/bin/activate
#     pip install -r requirements.txt
#     # then install the bundled DoRA-patched PEFT in editable mode:
#     pip install -e peft
#
# The bundled `peft/` directory is the DoRA fork required for the DoRA baseline
# (the FuRA / BlockTT path uses the shared `fura/` package instead and does not
# require the patched PEFT, but installing it keeps both baselines runnable).

torch==2.0.1
torchvision==0.15.2
transformers==4.31.0
tokenizers>=0.12.1,<0.14
sentencepiece==0.1.99
shortuuid
accelerate==0.21.0
bitsandbytes==0.41.0
pydantic<2,>=1
markdown2[all]
numpy
scikit-learn==1.2.2
gradio==3.35.2
gradio_client==0.2.9
requests
httpx==0.24.0
uvicorn
fastapi
einops==0.6.1
einops-exts==0.0.4
timm==0.6.13

# Training-time:
deepspeed==0.9.5
ninja
wandb
