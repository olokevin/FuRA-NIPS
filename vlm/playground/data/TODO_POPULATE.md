# TODO: populate this directory

Place the LLaVA visual instruction tuning dataset here:
* `llava_v1_5_mix665k.json` (mixture annotations)
* image folders per dataset (COCO/train2017, GQA/images, OCR-VQA, TextVQA, VisualGenome)

See `../../README.md` for download instructions.
