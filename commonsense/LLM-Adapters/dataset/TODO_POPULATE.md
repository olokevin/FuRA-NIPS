# TODO: populate this directory

Place the eight LLM-Adapters evaluation tasks here (one subdirectory each):
`boolq/`, `piqa/`, `social_i_qa/`, `hellaswag/`, `winogrande/`,
`ARC-Challenge/`, `ARC-Easy/`, `openbookqa/` — each containing `test.json`.

See `../SETUP.md` for download instructions.
